apiVersion: apps/v1
kind: Deployment
metadata:
  name: cc-backend-deployment
  namespace: cc-namespace
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cc-backend
  template:
    metadata:
      labels:
        app: cc-backend
    spec:
      containers:
      - name: cc-backend
        image: <your-dockerhub-username>/cc_backend:latest
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: cc-backend-service
  namespace: cc-namespace
spec:
  selector:
    app: cc-backend
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP